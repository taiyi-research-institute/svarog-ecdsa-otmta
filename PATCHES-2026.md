# 2026 年协议补丁与哈希编码 v2

本次修改覆盖单笔 `sign`、批量 `sign_batch`，以及二者共用的 DKG、PPRF、SoftSpoken。
签名继续使用现有四轮编排，保留提前生成 gadget 的 Variant III。
补丁来源是 [2026-976](https://eprint.iacr.org/2026/976) 与 [2026-929](https://eprint.iacr.org/2026/929)；下文区分论文要求与本仓库的编码约定。

## 2026-976：RVOLE

| 项目 | 改动 | 来源 |
| --- | --- | --- |
| 提前固定 gadget | OT 数量由 512 改为 2688，SoftSpoken 扩展长度为 2816，分块数为 21 | 附录 B.3、定理 B.17 |
| 指定输入 | 修正矩阵使用新采样的随机负载 w；同轮发送 δ = a − w，校验通过后 Receiver 加上 δb | §4.4 第 4 项 |
| Fiat–Shamir 挑战 | 绑定 Base OT、PPRF 初始化记录摘要，本次完整 SoftSpoken 消息，修正矩阵及其维度 | §4.5 |
| 一次性物料 | RVOLE 按值消费 SoftSpoken 输出，取消这两个输出类型的 Clone，随机负载不暴露给调用方 | 本仓库对一次性使用的约束 |

这里的目标计算安全参数为 128 位，统计参数也为 128 位；曲线标量宽度为 256 位。
原有基础 OT 的 `LAMBDA_C = 256` 保留，不能把这个宽度解读为 secp256k1 的整体计算安全强度。
参数测试直接检查定理 B.17 的两个不等式，并检查 SoftSpoken 的分块整除条件。

gadget、theta 与从 OT 种子扩展的标量改为拒绝采样：哈希得到 32 字节大端整数，只有小于群阶才接受，否则递增计数器重试。
这避免把随机 256 位整数直接模约减产生的偏差。
现有 sigma 保留为 64 字节摘要；哈希标签、逐元素派生和计数器格式是本仓库的实例化约定，论文没有规定这些字节格式。

OT 初始化摘要按方向保存：Base OT Receiver 消息、Base OT Sender 消息、PPRF 消息，依次纳入各列表长度和规范点编码。
签名期再加入本次 SoftSpoken 的 u、beta_tilde、t，以及协议参数，双方独立计算相同摘要。
秘密 OT 密钥、选择位及本地输出不属于公开记录，不能拿来作为双方共同的 transcript。

§4.5 对此修补有前提：具体 OT 的记录必须约束 Sender 的输入。
本实现保留 Endemic OT → PPRF → SoftSpoken 的具体结构，并绑定全部相关记录；这次修改没有将它替换成理想 Sender Random OT，也没有给该组合新增形式化安全证明。
验收测试检查记录绑定、消息篡改拒绝及输出关系，不能替代这项证明。

## 2026-929：最终 nonce 一致性

单笔 R4 部分签名消息增加完整聚合点 R；批量 R4 增加按签名顺序排列的 R 列表。
接收方在聚合前逐一比较完整点，任何不同都返回 `NonceEchoMismatch`。
论文在第三轮携带该点；本实现已有四轮，因此放在产生聚合点之后的第四轮，不额外增加轮次。

最终验证要求 `s⁻¹(mG + rQ) = R`，比较完整点，包含纵坐标符号。
原来的检查只比较横坐标，会同时接受对应于 R 与 −R 的签名；回归测试确认翻转 s 的符号会被拒绝。
零分母、零签名分量及单位元 nonce 也会被拒绝，失败后须用新会话和新随机数重试。

保留现有乘法输入与公开点检查、公开份额聚合检查和种子派生的零和掩码。
2026-929 的无预共享种子零分享、2026-976 的两方独立预处理是替代流程，本次没有加入这些新 API。

## 哈希参数边界与上下文

通用哈希编码、域分离与标量采样修正已在 main 提交，详见 [HASH-ENCODING.md](./HASH-ENCODING.md)。
本分支在其上增加签名上下文与协议记录绑定。
这些字节编码约定属于本仓库的实现选择。

签名哈希上下文绑定原会话号、初始化会话号、基础公钥、排序后的签名方、消息摘要、密钥偏移、批量顺序及单笔/批量模式。
传输仍使用原会话号路由，密码学计算使用该上下文，消息在 nonce 揭示之前便已固定。
调用方仍须保证会话号不复用；本库没有持久化的跨进程会话去重数据库。

## 兼容性与验证

v2 与旧消息、旧哈希承诺及旧 OT/PPRF 物料不兼容，参与方必须一同升级。
辅助密钥资料保留补丁必需的初始化摘要，初始化会话字段命名为 `keygen_sid`。
版本和兼容性由外部业务层管理，本库不在 aux 中重复保存协议版本。
外层须选择相应解析器并保证通信各方使用同一协议；旧资料缺少初始化摘要时不能补零继续签名。
不要丢弃旧私钥份额：需要保留公钥时，应通过重新执行 OT 初始化或经过验证的重分享流程生成新辅助资料。

验证命令：

```sh
cargo fmt --check
cargo test --locked --offline -- --test-threads=4
```

首次构建若尚未缓存依赖，先执行 `cargo test --locked --no-run` 下载锁定版本。
测试覆盖单笔及批量的多种门限组合、不同衍生偏移、重分享后签名，并包含上述篡改与编码回归测试。

本次验证结果：31 项测试全部通过，`cargo fmt --check` 与 `git diff --check` 通过。
