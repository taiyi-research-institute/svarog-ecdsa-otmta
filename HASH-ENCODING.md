# 哈希编码与域分离

所有协议哈希统一经过 `FramedHash`，每次 `update(bytes)` 表示一个完整参数。
参数编码为 `u64_be(bytes.len()) || bytes`，哈希流开头包含编码版本标签和输出长度。
`hash!` 宏采用相同编码，连续调用 `update` 时由封装保留参数边界。

列表先写元素个数，嵌套列表逐层写长度；索引使用明确宽度的整数，元素顺序固定。
固定分隔符可能出现在参数中，长度前缀避免了这种歧义。
测试覆盖相同拼接字节、分隔符内容、空参数、零字节，以及显式编码的对照结果。

Base OT 的密钥派生包含会话号，初始化调用区分 Sender 与 Receiver 的方向。
PPRF 派生绑定树、层、节点或叶子编号；DKG 承诺具有独立域分离标签。
gadget、挑战和 OT 扩展标量采用拒绝采样，避免直接模约减带来的偏差。

这些修改保留远端 main 的 keystore 字段和解析方式，版本由业务层管理。
哈希输出发生变化，通信各方须使用相同实现；已有会话和缓存哈希结果不能与旧实现混用。
论文 2026-929、2026-976 的协议修改单独维护在 patch2026 分支。
